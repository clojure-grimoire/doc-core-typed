{:ns "clojure.core.typed.test.edn-parser", :name "read-setting", :file "clojure/core/typed/test/edn_parser.clj", :type :fn, :src "(defn read-setting\n  \"reads a setting from the configuration file (defined by the environment\n   variable KODIAK_SETTINGS), loading it if necessary.\"\n  [skey]\n  (when-not @settings (load-settings))\n  (or (when-let [s @settings] (get s skey))\n      (get defaults skey)))", :column 1, :line 43, :arglists ([skey]), :doc "reads a setting from the configuration file (defined by the environment\n   variable KODIAK_SETTINGS), loading it if necessary."}
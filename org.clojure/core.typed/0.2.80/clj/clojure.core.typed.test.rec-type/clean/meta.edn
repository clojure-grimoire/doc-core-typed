{:ns "clojure.core.typed.test.rec-type", :name "clean", :file "clojure/core/typed/test/rec_type.clj", :type :fn, :src "(defn clean [rules data]\n  (reduce (t/ann-form (fn [cleaned-up kv]\n                      (let [rule-path (key kv)\n                            datum (val kv)]\n                        (if-let [rule (get rules rule-path)]\n                          (assoc cleaned-up rule-path datum)\n                          cleaned-up)))\n                    [Data (IMapEntry t/Any t/Any) -> Data])\n          {} data))", :column 1, :line 25, :arglists ([rules data])}
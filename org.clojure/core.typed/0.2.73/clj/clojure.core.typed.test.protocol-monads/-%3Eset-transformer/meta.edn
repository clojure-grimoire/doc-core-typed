{:type :fn, :src "(deftype set-transformer [m v]\n  clojure.lang.IDeref\n  (deref [_]\n    v)\n\n  Monad\n  (do-result [_ v]\n    (set-transformer. m (m (hash-set v))))\n  (bind [mv f]\n    (let [v (deref mv)]\n      (set-transformer. m (bind v (fn [xs]\n                                    (if (clojure.core/seq xs)\n                                      (->> xs\n                                           (map (comp deref f))\n                                           (fmap (partial apply lazy-concat)))\n                                      (m #{})))))))\n\n  MonadZero\n  (zero [_]\n    (set-transformer. m (m #{})))\n  (plus-step [mv mvs]\n    (set-transformer.\n     m (reduce (lift set/union)\n               (m #{})\n               (clojure.core/map deref (cons mv mvs))))))", :ns "clojure.core.typed.test.protocol-monads", :name "->set-transformer", :file "clojure/core/typed/test/protocol_monads.clj", :column 1, :line 561, :arglists ([m v]), :doc "Positional factory function for class clojure.core.typed.test.protocol_monads.set-transformer."}
{:type :fn, :src "(defn resolve-class-symbol [the-class]\n  `(impl/with-clojure-impl\n     (let [cls# (when-let [c# (resolve ~the-class)]\n                  (when (class? c#)\n                    c#))]\n       (assert cls# (str \"Cannot resolve class \" ~the-class))\n       (or (and cls# (coerce/Class->symbol cls#))\n           ~the-class))))", :ns "clojure.core.typed.base-env-helper", :name "resolve-class-symbol", :file "clojure/core/typed/base_env_helper.clj", :column 1, :line 125, :arglists ([the-class])}
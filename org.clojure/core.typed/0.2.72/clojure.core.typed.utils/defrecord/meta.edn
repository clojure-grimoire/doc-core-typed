{:type :macro, :src "(defmacro defrecord [name slots inv-description invariants & etc]\n  ;only define record if symbol doesn't resolve, not completely sure if this behaves like defonce\n  (when-not (resolve name)\n    `(contracts/defconstrainedrecord ~name ~slots ~inv-description ~invariants ~@etc)))", :macro true, :ns "clojure.core.typed.utils", :name "defrecord", :file "clojure/core/typed/utils.clj", :column 1, :line 82, :arglists ([name slots inv-description invariants & etc])}
{:ns "clojure.core.typed.tvar-env", :name "with-extended-new-tvars", :file "clojure/core/typed/tvar_env.clj", :type :macro, :src "(defmacro with-extended-new-tvars\n  \"Extends with new type variables (provided by (e.g., Poly-fresh))\"\n  [vars fresh-vars & body]\n  `(binding [*current-tvars* (extend-many *current-tvars* ~vars ~fresh-vars)]\n     ~@body))", :column 1, :line 44, :macro true, :arglists ([vars fresh-vars & body]), :doc "Extends with new type variables (provided by (e.g., Poly-fresh))"}
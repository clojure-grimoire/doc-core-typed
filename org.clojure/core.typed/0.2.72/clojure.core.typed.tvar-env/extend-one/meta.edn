{:type :fn, :src "(defn extend-one\n  \"Extend a tvar environment. Adds an entry mapping var to itself,\n  or if fresh-var is provided, mapping var to fresh-var\"\n  ([env var] (extend-one env var nil))\n  ([env var fresh-var]\n   {:pre [(tvar-env? env)\n          (symbol? var)\n          ((some-fn symbol? nil?) fresh-var)]\n    :post [(tvar-env? %)]}\n   (assoc env var (r/make-F (or fresh-var var)))))", :ns "clojure.core.typed.tvar-env", :name "extend-one", :file "clojure/core/typed/tvar_env.clj", :column 1, :line 65, :arglists ([env var] [env var fresh-var]), :doc "Extend a tvar environment. Adds an entry mapping var to itself,\n  or if fresh-var is provided, mapping var to fresh-var"}
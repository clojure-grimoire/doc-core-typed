{:ns "clojure.core.typed.test.poly", :name "repeatedly'", :file "clojure/core/typed/test/poly.clj", :type :fn, :src "(defn repeatedly'\n  \"Takes a function of no args, presumably with side effects, and\n  returns an infinite (or length n if supplied) lazy sequence of calls\n  to it\"\n  ([f] (lazy-seq (cons (f) (repeatedly' f))))\n  ([n f] (take n (repeatedly' f))))", :column 1, :line 9, :arglists ([f] [n f]), :doc "Takes a function of no args, presumably with side effects, and\n  returns an infinite (or length n if supplied) lazy sequence of calls\n  to it"}
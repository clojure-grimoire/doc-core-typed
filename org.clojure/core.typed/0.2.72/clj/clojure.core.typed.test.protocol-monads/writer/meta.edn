{:ns "clojure.core.typed.test.protocol-monads", :name "writer", :file "clojure/core/typed/test/protocol_monads.clj", :type :fn, :src "(defn writer\n  \"Monad describing computations that accumulate data on the side, e.g. for\n   logging. The monadic values have the structure [value log]. Any of the\n   accumulators from clojure.contrib.accumulators can be used for storing the\n   log data. Its empty value is passed as a parameter.\"\n  [accumulator]\n  (fn [v]\n    (writer-monad. v accumulator)))", :column 1, :line 399, :arglists ([accumulator]), :doc "Monad describing computations that accumulate data on the side, e.g. for\n   logging. The monadic values have the structure [value log]. Any of the\n   accumulators from clojure.contrib.accumulators can be used for storing the\n   log data. Its empty value is passed as a parameter."}
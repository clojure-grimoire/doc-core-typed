{:ns "clojure.core.typed.test.protocol-monads", :name "set-t", :file "clojure/core/typed/test/protocol_monads.clj", :type :fn, :src "(defn set-t\n  \"monad transformer that transforms a monad m into a monad in which\n   the base values are sets.\"\n  [m]\n  (fn [v]\n    (set-transformer. m (m (hash-set v)))))", :column 1, :line 587, :arglists ([m]), :doc "monad transformer that transforms a monad m into a monad in which\n   the base values are sets."}
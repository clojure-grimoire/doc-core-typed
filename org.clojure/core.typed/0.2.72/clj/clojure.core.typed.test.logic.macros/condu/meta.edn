{:ns "clojure.core.typed.test.logic.macros", :name "condu", :file "clojure/core/typed/test/logic/macros.clj", :type :macro, :src "(defmacro condu\n  \"Committed choice. Once the head (first goal) of a clause \n  has succeeded, remaining goals of the clause will only\n  be run once. Non-relational.\"\n  [& clauses]\n  (let [a (gensym \"a\")]\n    `(fn [~a]\n       (ifu* ~@(map (cond-clauses a) clauses)))))", :column 1, :line 210, :macro true, :arglists ([& clauses]), :doc "Committed choice. Once the head (first goal) of a clause \n  has succeeded, remaining goals of the clause will only\n  be run once. Non-relational."}
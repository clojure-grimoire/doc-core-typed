{:type :fn, :src "(defn visit-bounds \n  \"Apply f to each element of bounds\"\n  [ty f]\n  {:pre [(Bounds? ty)]\n   :post [(Bounds? ty)]}\n  (-> ty\n    (update-in [:upper-bound] #(when %\n                                 (f %)))\n    (update-in [:lower-bound] #(when %\n                                 (f %)))\n    (update-in [:higher-kind] #(when %\n                                 (f %)))))", :ns "clojure.core.typed.type-rep", :name "visit-bounds", :file "clojure/core/typed/type_rep.clj", :column 1, :line 989, :arglists ([ty f]), :doc "Apply f to each element of bounds"}
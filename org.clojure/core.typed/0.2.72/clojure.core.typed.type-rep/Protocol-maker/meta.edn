{:type :fn, :src "(u/def-type Protocol [the-var variances poly? on-class methods declared?]\n  \"A Clojure Protocol\"\n  [(symbol? the-var)\n   (or (nil? variances)\n       (and (seq variances)\n            (every? variance? variances)))\n   (or (nil? poly?)\n       (and (seq poly?)\n            (every? (some-fn Scope? Type?) poly?)))\n   (= (count poly?) (count variances))\n   (symbol? on-class)\n   ((con/hash-c? (every-pred symbol? (complement namespace)) (some-fn Scope? Type?)) methods)\n   (con/boolean? declared?)]\n  :methods\n  [p/TCType])", :ns "clojure.core.typed.type-rep", :name "Protocol-maker", :file "clojure/core/typed/type_rep.clj", :column 1, :line 290, :arglists ([the-var variances poly? on-class methods declared? & {meta__2330__auto__ :meta, :as opt__2331__auto__}])}
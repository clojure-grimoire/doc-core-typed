{:ns "clojure.core.typed.tvar-bnds", :name "with-extended-bnds", :file "clojure/core/typed/tvar_bnds.clj", :type :macro, :src "(defmacro with-extended-bnds\n  \"Takes a list of vars and bnds extends the current tvar environment.\n  vars are the fresh names of the frees, rather than the scoped names.\"\n  [vars bndss & body]\n  `(binding [*current-tvar-bnds* (extend-many *current-tvar-bnds* ~vars ~bndss)]\n     ~@body))", :column 1, :line 58, :macro true, :arglists ([vars bndss & body]), :doc "Takes a list of vars and bnds extends the current tvar environment.\n  vars are the fresh names of the frees, rather than the scoped names."}
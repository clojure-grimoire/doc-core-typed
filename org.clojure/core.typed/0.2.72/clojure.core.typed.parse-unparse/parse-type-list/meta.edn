{:type :var, :src "(defmulti parse-type-list \n  (fn [[n]]\n    {:post [((some-fn nil? symbol?) %)]}\n    (when (symbol? n)\n      (or (impl/impl-case\n            :clojure (cond\n                       (special-symbol? n) n\n                       :else (let [r (resolve-type-clj n)]\n                               (when (var? r)\n                                 (coerce/var->symbol r))))\n            :cljs (resolve-type-cljs n))\n          n))))", :ns "clojure.core.typed.parse-unparse", :name "parse-type-list", :file "clojure/core/typed/parse_unparse.clj", :column 1, :line 83}
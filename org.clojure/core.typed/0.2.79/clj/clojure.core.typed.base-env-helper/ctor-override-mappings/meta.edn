{:type :macro, :src "(defmacro ctor-override-mappings [& args]\n  `(impl/with-clojure-impl\n     (let [ts# (partition 2 '~args)]\n       (into {}\n             (doall\n               (for [[s# t#] ts#]\n                 (do\n                   (assert (and (symbol? s#)\n                                (not (namespace s#)))\n                           \"Need unqualified symbol\")\n                   [s# (prs/parse-type t#)])))))))", :macro true, :ns "clojure.core.typed.base-env-helper", :name "ctor-override-mappings", :file "clojure/core/typed/base_env_helper.clj", :column 1, :line 91, :arglists ([& args])}
{:ns "clojure.core.typed.test.protocol-monads", :name "vector-t", :file "clojure/core/typed/test/protocol_monads.clj", :type :fn, :src "(defn vector-t\n  \"monad transformer that transforms a monad m into a monad in which\n   the base values are vectors.\"\n  [m]\n  (fn [v]\n    (vector-transformer. m (m (vector v)))))", :column 1, :line 553, :arglists ([m]), :doc "monad transformer that transforms a monad m into a monad in which\n   the base values are vectors."}
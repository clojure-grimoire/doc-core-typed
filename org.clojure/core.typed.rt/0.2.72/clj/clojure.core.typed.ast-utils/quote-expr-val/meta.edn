{:type :fn, :src "(defn quote-expr-val [{:keys [op expr] :as q}]\n  {:pre [(or (and (#{:quote} op)\n                  (#{:const} (:op expr)))\n             (#{:const} op))]}\n  (if (#{:quote} op)\n    (:val expr)\n    (:val q)))", :ns "clojure.core.typed.ast-utils", :name "quote-expr-val", :file "clojure/core/typed/ast_utils.clj", :column 1, :line 46, :arglists ([{:keys [op expr], :as q}])}
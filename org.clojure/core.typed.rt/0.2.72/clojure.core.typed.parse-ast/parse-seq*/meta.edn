{:type :var, :src "(defmulti parse-seq*\n  (fn [[n]]\n    {:post [((some-fn nil? symbol?) %)]}\n    (when (symbol? n)\n      (or (impl/impl-case\n            :clojure (cond\n                       (special-symbol? n) n\n                       :else (let [r (resolve-type-clj n)]\n                               (when (var? r)\n                                 (coerce/var->symbol r))))\n            :cljs n)\n          n))))", :ns "clojure.core.typed.parse-ast", :name "parse-seq*", :file "clojure/core/typed/parse_ast.clj", :column 1, :line 467}
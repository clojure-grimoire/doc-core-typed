{:type :fn, :src "(defn parse-filter-set [{:keys [then else] :as fsyn}]\n  {:op :filter-set\n   :then (if then\n           (parse-filter then)\n           {:op :top-filter})\n   :else (if else\n           (parse-filter else)\n           {:op :top-filter})})", :ns "clojure.core.typed.parse-ast", :name "parse-filter-set", :file "clojure/core/typed/parse_ast.clj", :column 1, :line 98, :arglists ([{:keys [then else], :as fsyn}])}
{:type :fn, :src "(defn multi-frequencies \n  \"Like frequencies, but only returns frequencies greater\n  than one\"\n  [coll]\n  (->> coll\n       frequencies\n       (filter (fn [[k freq]]\n                 (when (< 1 freq)\n                   true)))\n       (into {})))", :ns "clojure.core.typed.parse-ast", :name "multi-frequencies", :file "clojure/core/typed/parse_ast.clj", :column 1, :line 359, :arglists ([coll]), :doc "Like frequencies, but only returns frequencies greater\n  than one"}
{:type :fn, :src "(defn env-for-error [env]\n  ; impl-case probably can't be done here\n  (merge (select-keys env [:line :column])\n         ;clojure specific\n         (let [f (:file env)]\n           (when (string? f)\n             {:file f}))\n         ;cljs specific\n         ;FIXME filename?\n         (let [n (get-in env [:ns :name])]\n           (when (symbol? n)\n             {:ns n}))))", :ns "clojure.core.typed.errors", :name "env-for-error", :file "clojure/core/typed/errors.clj", :column 1, :line 22, :arglists ([env])}